import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Login | Smart Wardrobe AI",
  description: "Log in to your Smart Wardrobe account",
};

export default function LoginPage() {
  return <LoginForm />;
}
